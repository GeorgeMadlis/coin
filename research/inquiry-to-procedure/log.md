# Omitted Source-Repository Log

This path is retained because the committed `framework-self` bundle links to the original source repository's root `log.md`.

The original root ledger was not copied into COIN. The public evidence for this research trail is the committed bundle content under `bundles/`, especially each bundle's own `log.md` and `inquiry/` records.
