# Omitted Source Specification

This path is retained because the committed `framework-self` bundle links to the original source repository's `spec/SPEC.md`.

The full source specification was not copied into COIN. The public evidence for this research trail is the committed bundle snapshot and its reproduction metadata, which cite the source specification as provenance.
