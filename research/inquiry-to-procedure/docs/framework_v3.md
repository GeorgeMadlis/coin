# Omitted Source Document: framework_v3.md

This path is retained because the committed `framework-self` bundle links to `docs/framework_v3.md` in the original private source repository.

That source document was not copied into COIN. The link resolves here so public readers can see that the reference is a preserved provenance pointer, not an available in-repo source.
