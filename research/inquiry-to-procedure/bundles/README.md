# Committed Bundles

This directory contains public, trimmed bundle snapshots used as evidence for the inquiry-to-procedure research trail.

The original source repositories are private. Bundle links that point outside the committed snapshot are preserved as provenance references and resolve to omission notes where the referenced private source file was not copied into COIN.
