# Omitted Source Tooling

This directory is retained because the committed `framework-self` bundle links to the original source repository's `tools/` directory.

The source tooling was not copied into COIN. The public research trail keeps the reference as provenance while relying on the committed bundle files for inspectable evidence.
